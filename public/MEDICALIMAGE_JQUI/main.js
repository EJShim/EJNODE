$( function() {
  //
  // $( "#e_mainView" ).resizable({
  //   //containment: "#e_subView"
  //   //alsoResize : "#e_subView"
  // });


  $( ".ec_subView" ).resizable({
    //alsoResize : "#e_mainView"
    minHeight: 150,
    minWidth: 200
  });
} );
