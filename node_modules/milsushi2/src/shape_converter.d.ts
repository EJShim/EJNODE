import Matrix = require('./matrix');
export declare function transpose(A: Matrix): Matrix;
export declare function repmat(A: Matrix, ...rs: number[]): Matrix;
export declare function repmat(A: Matrix, rs: number[]): Matrix;
export declare function repmat(A: Matrix, r: Matrix): Matrix;
export declare function cat(dim: number, ...As: Matrix[]): Matrix;
export declare function horzcat(...As: Matrix[]): Matrix;
export declare function vertcat(...As: Matrix[]): Matrix;
export declare function permute(A: Matrix, order: number[]): Matrix;
export declare function ipermute(A: Matrix, order: number[]): Matrix;
