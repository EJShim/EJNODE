import Matrix = require('./matrix');
import util = require('./util');
export declare type MatrixOrNumber = util.MatrixOrNumber;
export declare function max(A: MatrixOrNumber, B?: MatrixOrNumber, dim?: number): Matrix;
export declare function min(A: MatrixOrNumber, B?: MatrixOrNumber, dim?: number): Matrix;
export declare function argmax(A: MatrixOrNumber, dummy?: any, dim?: number): {
    M: Matrix;
    I: Matrix;
};
export declare function argmin(A: MatrixOrNumber, dummy?: any, dim?: number): {
    M: Matrix;
    I: Matrix;
};
export declare function sum(A: Matrix): Matrix;
export declare function sum(A: Matrix, dim: number, outtype?: string): Matrix;
export declare function sum(A: Matrix, outtype?: string): Matrix;
export declare function mean(A: Matrix): Matrix;
export declare function mean(A: Matrix, dim: number, outtype?: string): Matrix;
export declare function mean(A: Matrix, outtype?: string): Matrix;
export declare function prod(A: Matrix): Matrix;
export declare function prod(A: Matrix, dim: number, outtype?: string): Matrix;
export declare function prod(A: Matrix, outtype?: string): Matrix;
export declare function variance(A: Matrix, w?: number, dim?: number): Matrix;
export declare function std(A: Matrix, w?: number, dim?: number): Matrix;
