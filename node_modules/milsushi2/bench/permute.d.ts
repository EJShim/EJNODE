import $M = require('../src/sushi');
import BenchBase = require('./bench_base');
declare class permute extends BenchBase {
    shape: number[];
    order: number[];
    constructor(shape: number[], order: number[]);
    setup(): $M.Matrix[];
    run(a: $M.Matrix): void;
}
export = permute;
