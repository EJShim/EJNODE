import $M = require('../src/sushi');
import BenchBase = require('./bench_base');
declare class mtimes extends BenchBase {
    m: number;
    n: number;
    k: number;
    constructor(m: number, n: number, k: number);
    setup(): $M.Matrix[];
    run(a: $M.Matrix, b: $M.Matrix): void;
}
export = mtimes;
