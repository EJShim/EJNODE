"use strict";
var __extends = (this && this.__extends) || function (d, b) {
    for (var p in b) if (b.hasOwnProperty(p)) d[p] = b[p];
    function __() { this.constructor = d; }
    d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
};
var $M = require('../src/sushi');
var BenchBase = require('./bench_base');
var destruct = (function (_super) {
    __extends(destruct, _super);
    function destruct(m) {
        _super.call(this);
        this.m = m;
        this.name = "destruct " + m;
    }
    destruct.prototype.setup = function () {
        this.mats = [];
        for (var i = 0; i < 10; i++) {
            this.mats.push($M.gpuArray($M.rand(this.m, 1)));
        }
        return [];
    };
    destruct.prototype.run = function () {
        var mat = this.mats.pop();
        mat.destruct();
    };
    return destruct;
}(BenchBase));
module.exports = destruct;
//# sourceMappingURL=destruct.js.map