import $M = require('../src/sushi');
import BenchBase = require('./bench_base');
declare class transpose extends BenchBase {
    m: number;
    n: number;
    constructor(m: number, n: number);
    setup(): $M.Matrix[];
    run(a: $M.Matrix): void;
}
export = transpose;
