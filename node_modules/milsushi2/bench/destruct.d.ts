import $M = require('../src/sushi');
import BenchBase = require('./bench_base');
declare class destruct extends BenchBase {
    m: number;
    mats: $M.Matrix[];
    constructor(m: number);
    setup(): any[];
    run(): void;
}
export = destruct;
