import $M = require('../src/sushi');
import BenchBase = require('./bench_base');
declare class mtimes_trans extends BenchBase {
    m: number;
    n: number;
    k: number;
    trans_a: boolean;
    trans_b: boolean;
    constructor(m: number, n: number, k: number, trans_a: boolean, trans_b: boolean);
    setup(): $M.Matrix[];
    run(a: $M.Matrix, b: $M.Matrix): void;
}
export = mtimes_trans;
