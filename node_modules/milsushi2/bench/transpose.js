"use strict";
var __extends = (this && this.__extends) || function (d, b) {
    for (var p in b) if (b.hasOwnProperty(p)) d[p] = b[p];
    function __() { this.constructor = d; }
    d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
};
var $M = require('../src/sushi');
var BenchBase = require('./bench_base');
var transpose = (function (_super) {
    __extends(transpose, _super);
    function transpose(m, n) {
        _super.call(this);
        this.m = m;
        this.n = n;
        this.name = "transpose " + m + "*" + n;
    }
    transpose.prototype.setup = function () {
        var a = $M.gpuArray($M.rand(this.m, this.n));
        return [a];
    };
    transpose.prototype.run = function (a) {
        $M.t(a);
    };
    return transpose;
}(BenchBase));
module.exports = transpose;
//# sourceMappingURL=transpose.js.map