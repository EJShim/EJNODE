"use strict";
var __extends = (this && this.__extends) || function (d, b) {
    for (var p in b) if (b.hasOwnProperty(p)) d[p] = b[p];
    function __() { this.constructor = d; }
    d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
};
var $M = require('../src/sushi');
var BenchBase = require('./bench_base');
var permute = (function (_super) {
    __extends(permute, _super);
    function permute(shape, order) {
        _super.call(this);
        this.shape = shape;
        this.order = order;
        this.name = "permute ([" + shape + "], [" + order + "])";
    }
    permute.prototype.setup = function () {
        var a = $M.zeros.apply($M, this.shape.concat(['gpuArray']));
        return [a];
    };
    permute.prototype.run = function (a) {
        $M.permute(a, this.order);
    };
    return permute;
}(BenchBase));
module.exports = permute;
//# sourceMappingURL=permute.js.map